{
  "title": "Transformer Settings",
  "description": "The settings for Transformer.",
  "properties": {
    "transformerConfig": {
      "title": "Transformer Configuration",
      "description": "The configuration for transformer.",
      "$ref": "#/definitions/transformerConfig",
      "default": {
        "enabled": true
      }
    }
  },
  "additionalProperties": false,
  "type": "object",
  "definitions": {
    "transformerConfig": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "title": "Enable transformer"
        },
        "notebookDir": {
          "type": "string",
          "default": "",
          "title": "Root directory of the notebook instance."
        },
        "proxyUrl": {
          "type": "string",
          "default": "",
          "title": "URL of the proxy."
        }
      },
      "additionalProperties": false,
      "type": "object"
    }
  }
}

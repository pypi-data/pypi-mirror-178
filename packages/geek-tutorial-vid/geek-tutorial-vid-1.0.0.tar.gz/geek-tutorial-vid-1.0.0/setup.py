from setuptools import setup, find_packages

setup(name="geek-tutorial-vid", version='1.0.0', author='geek', description='simple tutorial package', packages=find_packages(), install_requires=['cloudscraper'])
from setuptools import setup, find_packages

setup(name="geek-tutorial-vid", version='1.0.2', author='geek', description='simple tutorial package', long_description='simple python package test', packages=["modes"], install_requires=['cloudscraper'])
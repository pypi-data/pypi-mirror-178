[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100

[tool.mypy]
python_version = "3.6"

[tool.pylint.messages]
disable = [
  "duplicate-code",
]

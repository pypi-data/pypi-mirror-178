[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rand_convolve"
version = "1.0"
description = "Runs a convolution on an image with random kernels."
dependencies = [
    "PIL >= 4.3.0",
    "scipy > 1.5.0",
    "numpy"
]
readme = "README.md"
authors = [{ name = "aceynk" }]
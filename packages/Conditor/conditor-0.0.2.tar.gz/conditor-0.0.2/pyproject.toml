
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "conditor"
version = "0.0.2"

authors = [    { name = 'Sophia Roger Hürzeler', email ='srh@srhuerzeler.net' }]

description = "Project management utility."
readme = "README.rst"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License"
]

[project.urls]
"GitLab repository" = "https://gitlab.com/srhuerzeler/conditor"
"PyPI package" = "https://pypi.org/project/conditor"

[project.scripts]
conditor = 'conditor.__main__:main'


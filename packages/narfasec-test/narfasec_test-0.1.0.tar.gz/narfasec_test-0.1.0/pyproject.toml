[tool.poetry]
name = "narfasec-test"
version = "0.1.0"
description = "Just a test"
authors = ["narfasec"]
readme = "README.md"
packages = [{include = "src/example_package_narfasec"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
sherlog = 'entry:main'
[tool.poetry]
name = "PFATools"
version = "0.1.0"
description = ""
authors = ["msherburne <mathieusherburne@outlook.com>"]
packages = [{include = "pfatools"}]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

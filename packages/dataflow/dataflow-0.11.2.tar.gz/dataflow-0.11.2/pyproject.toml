[build-system]
requires = ["setuptools", "wheel", "versioneer-518"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q --cov-report term"
testpaths = [
    "tests",
]

[tool.coverage.report]
ignore_errors = true

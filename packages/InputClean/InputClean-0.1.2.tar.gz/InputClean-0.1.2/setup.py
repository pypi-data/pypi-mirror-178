from setuptools import setup, find_packages

setup(
    name = "InputClean",
    version = "0.1.2",
    keywords = ["pip", "InputClean"],
    description = "Auto del input noise.",
    long_description = "Auto del input noise.", 
    author = "KurehaSHO",
    packages = find_packages(),
    include_package_data = True,
    platforms = "any" 
)

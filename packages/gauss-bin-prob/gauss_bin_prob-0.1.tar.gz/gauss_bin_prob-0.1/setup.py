from setuptools import setup

setup(name='gauss_bin_prob',
      version='0.1',
      author='github.com/Wakhie',
      description='Gaussian and Binomial probability distributions',
      packages=['distributions'],
      zip_safe=False,
      python_requires='>=3')

[tool.poetry]
name = "qattools"
version = "0.3.2"
description = ""
authors = ["Richard Hajek <richard.m.hajek@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
pandas = "^1.0.0"
torch = "^1.0.0"
torchinfo = "^1.0.0"

[tool.poetry.group.dev.dependencies]
torchvision = "^0.14.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "calculator_ab"
authors = [{name = "Andrius_B", email = "andrius@b.org"}]
dynamic = ["version", "description"]

description-file = "README.md"

requires-python = ">=3.7"
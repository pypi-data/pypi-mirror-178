[tool.poetry]
name = "{{ project_name }}"
version = "0.0.1"
description = "A new Cstack project!"
authors = []
license = ""
readme = ""

[tool.poetry.dependencies]
python = "^3.11"
cstack = "{{ version }}"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
black = "^22.10.0"
isort = "^5.10.1"
pyflakes = "^3.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

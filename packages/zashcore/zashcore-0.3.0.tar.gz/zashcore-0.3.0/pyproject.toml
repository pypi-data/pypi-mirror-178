[tool.poetry]
name = "zashcore"
version = "0.3.0"
description = "Core libs for zash"
authors = ["Efe <efe@joinzash.com>"]

[tool.poetry.dependencies]
python = "^3.9"
structlog = "^22.2.0"

[tool.poetry.dev-dependencies]
pytest = "^7.2.0"
rich = "^12.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

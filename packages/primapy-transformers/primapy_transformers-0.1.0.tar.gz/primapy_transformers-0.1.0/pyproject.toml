[tool.poetry]
name = "primapy-transformers"
version = "0.1.0"
description = "Safety placeholder"
authors = ["Team AMOps <amops@prima.it>"]
readme = "README.md"
packages = [{include = "primapy_transformers", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
black = "^22.10.0"
isort = "^5.10.1"
pylint = "^2.15.5"
mypy = "^0.991"
toml = "^0.10.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
